api_origin: "https://internal-api-space.feishu.cn"
initial_table_id: "tblkKIbEMShri8pB"
initial_view_id: "vew4vJvx97"

# This is a one-time foundation backfill. It must never be added to the 07:30 task.
# The real page URL is read from XIAOZHAOYA_FEISHU_BASE_URL in local/server .env.
page_size: 3000
minimum_items: 1000
